server:
  port: 8848
spring:
  application:
    name: weather-config-client
  cloud:
    config:
      profile: dev
      uri: http://localhost:9999/
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8084/eureka/
